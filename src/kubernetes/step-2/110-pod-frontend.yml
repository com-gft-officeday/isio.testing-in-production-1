---

apiVersion: v1
kind: Pod
metadata:
  name: frontend
  namespace: com-gft-deuba-unity-release-1
  labels:
    demo/project: com.gft.deuba.unity.release-1
    demo/app: step-2
    demo/module: frontend
spec:
  containers:
    - name: frontend
      image: com.gft.deuba.unity.release-1.frontend.fe-measure:v1
      args: [ "--name", "frontend-1", "--device", "http://thermometer/measure" ]
      imagePullPolicy: Never
      resources:
        limits:
          memory: "128Mi"
          cpu: "500m"
