---

apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: step-3
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    demo/project: com.gft.deuba.unity.release-1
    demo/app: step-3
    demo/module: frontend
